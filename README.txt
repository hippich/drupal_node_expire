This module lets users set expiration dates for nodes in the create and edit
forms.

Assumes that the cron functionality is available.

Installation
------------

1. Copy the node_expire directory into your Drupal modules directory.

2. Go to administer >> configuration >> modules and enable the node_expire module.
